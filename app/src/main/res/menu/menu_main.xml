<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <item android:id="@+id/action_settings"
        android:title="@string/settings"
        android:orderInCategory="100"
        app:showAsAction="never"/>

    <item android:id="@+id/action_display_about"
        android:title="@string/about_menu"
        android:orderInCategory="100"
        android:onClick="onOptionItemSelected"
        app:showAsAction="always"/>

    <item android:id="@+id/action_cart"
        android:orderInCategory="0"
        android:title="@string/shopping_cart"
        app:showAsAction="always"
        android:icon="@drawable/ic_action_cart"/>

</menu>